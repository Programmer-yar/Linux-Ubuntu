-> "export" command:
	- used to export 'environment variables' to child processes
	- View all exported variables of current shell:
	  export -p
	- set up an environment variable
	  export <variable_name> = <value>
